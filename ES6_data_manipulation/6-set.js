/**
 * Creates a Set from an array.
 * @param {Array} array - An array of any kind of element.
 * @returns {Set} A Set containing the unique elements of the array.
 */
export default function setFromArray(array) {
  return new Set(array);
}
